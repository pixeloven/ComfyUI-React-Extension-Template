import os
import server
from aiohttp import web
import folder_paths

NODE_CLASS_MAPPINGS = {}
__all__ = ['NODE_CLASS_MAPPINGS']

WEB_DIRECTORY = 'dist/example_ext'

# # Define the path to our extension
# workspace_path = os.path.dirname(__file__)
# # Path to the compiled React app
# dist_path = os.path.join(workspace_path, 'dist/example_ext')

# # Register the static route to serve our compiled React app
# if os.path.exists(dist_path):
#     server.PromptServer.instance.app.add_routes([
#         web.static('/example_ext/', dist_path),
#     ])
#     print(f"✅ ComfyUI Example React Extension: Web directory registered at {dist_path}")
# else:
#     print(f"❌ ComfyUI Example React Extension: Web directory not found: {dist_path}")